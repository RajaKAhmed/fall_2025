apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-blue
  namespace: cuny-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
      version: blue
  template:
    metadata:
      labels:
        app: web
        version: blue
    spec:
      containers:
        - name: web-container
          image: nginx:1.19
